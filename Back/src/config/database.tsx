import mongoose from 'mongoose';

export const connectDB = async () => {
  tr
    const conn = await mongoose.conrocess.env.MONGO_URI || '');
    console.log(`📡 Cluster Alphnectado: ${conn.cnnion.host}`);
  } catch
